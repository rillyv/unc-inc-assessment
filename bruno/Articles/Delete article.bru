meta {
  name: Delete article
  type: http
  seq: 5
}

delete {
  url: {{API_BASE_URL}}/api/articles/42
  body: json
  auth: bearer
}

auth:bearer {
  token: {{session_token}}
}

settings {
  encodeUrl: true
}
