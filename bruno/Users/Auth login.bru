meta {
  name: Auth login
  type: http
  seq: 1
}

post {
  url: {{API_BASE_URL}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@example.com",
    "password": "password"
  }
}

settings {
  encodeUrl: true
}
