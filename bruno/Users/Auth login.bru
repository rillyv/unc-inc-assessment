meta {
  name: Auth login
  type: http
  seq: 1
}

post {
  url: {{API_BASE_URL}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@example.com",
    "password": "password"
  }
}

vars:post-response {
  session_token: res.body.token
}

settings {
  encodeUrl: true
}
